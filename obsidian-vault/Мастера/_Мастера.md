---
type: meta
---

# Мастера

Заметки в этой папке (`<Имя> (<id8>).md`) генерирует `scripts/sync-obsidian.ps1` — не
редактируйте их руками, правки перезатрутся при следующей синхронизации. Инструкция по
синхронизации — в README.md в корне vault.

Сейчас здесь пусто — мастеров в системе ещё не создавали (создаются SUPERADMIN'ом через
`POST /api/v1/users` + `POST /api/v1/users/{id}/master-profile`). Появятся после первого
запуска `./scripts/sync-obsidian.ps1` на backend, где уже есть хотя бы один мастер.
