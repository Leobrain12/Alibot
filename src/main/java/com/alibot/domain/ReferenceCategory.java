package com.alibot.domain;

/** ТЗ п.18.1/132 — справочники backend'а, не хардкод в клиентах (боте/Mini App). */
public enum ReferenceCategory {
    APPLIANCE_TYPE,
    BRAND,
    TIME_SLOT,
    MASTER_DECLINE_REASON,
    CUSTOMER_CANCEL_REASON,
    RESCHEDULE_REASON
}
