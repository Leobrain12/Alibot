package com.alibot.domain;

/** ТЗ п.10-11 — жизненный цикл лида: пришёл -> либо превращён в Order, либо отклонён (спам,
 *  дубль, недозвон и т.п.). Нет промежуточного "в работе" — квалификация лида и есть конвертация. */
public enum LeadStatus {
    NEW,
    CONVERTED,
    REJECTED
}
